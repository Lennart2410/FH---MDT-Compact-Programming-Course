package HomeworkAssignment2.loading;

import HomeworkAssignment2.general.Order;
import HomeworkAssignment2.general.Task;

public class LoadingTask extends Task {

    // Specific content of the LoadingTask
    // Should contain attributes which only accounts to the LoadingTask and not the generic Task

    public LoadingTask(Order order) {
        super(order);
    }
}
